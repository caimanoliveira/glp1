# Supabase (opcional). Sem estas variáveis, o app roda em modo local
# (dados no localStorage do navegador) — ótimo para uso pessoal single-device.
# Preencha para sincronizar via Supabase (Postgres + Auth + RLS).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
